<Window x:Class="WarehouseProject.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseProject.Views"
        mc:Ignorable="d"
        Title="LoginView" d:DesignHeight="500" d:DesignWidth="800" Background="#012e37">
    <Border  CornerRadius="50" Margin="60">
        <StackPanel Margin="20">
            <Label Content="User Input" FontSize="25" Foreground="#fff"
                   HorizontalAlignment="Center" FontWeight="Bold" />
            <Separator></Separator>
            <Label Content="Username" Foreground="#fff" FontSize="20" FontWeight="DemiBold"/>
            <TextBlock Name="txtUsername" Text="{Binding Username, ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged }" 
                       Background="#1c37e7" Height="40" Width="400" Margin="0,0,220,0"/>
            <Label Content="Password" Foreground="#fff" FontSize="20" FontWeight="DemiBold"/>
            <TextBlock Name="txtPassword" Background="#1c37e7" Height="40" Width="400" Margin="0,0,220,0"/>
            <Button x:Name="btnSubmit" Content="Submit"
                    Margin="60 20" Background="Azure" 
                    Foreground="#061262" FontSize="25" FontWeight="Bold" Command="" Height="40" Width="200"/>
        </StackPanel>      
    </Border>
  
</Window>